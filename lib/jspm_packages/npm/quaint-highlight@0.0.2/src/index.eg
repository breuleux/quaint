
require:
   .hl
   "highlight.js" as hljs

main{*match} =
   {[@] and {is-quaint-engine => true?}, options = {=}} ->
      options.hljs or= hljs
      hl{@, options}
   {options = {=}} ->
      {@} -> main{@, options}

provide = main
